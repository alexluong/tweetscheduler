{
  "name": "tweetscheduler",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "workspaces": [
    "client",
    "server",
    "database",
    "jobs"
  ],
  "scripts": {
    "start:client": "yarn workspace @ts/client start",
    "start:server": "yarn workspace @ts/server start",
    "build:client": "yarn workspace @ts/client build",
    "build:server": "yarn workspace @ts/server build",
    "db": "yarn workspace @ts/database db"
  },
  "dependencies": {
    "netlify-lambda": "^1.6.3"
  },
  "devDependencies": {}
}
